export interface ILocale {
  home: {
    title: string;
    culinar: {
      shop: string;
      tooltip: string;
    }
    tableStoreProduct: {
      name: string;
      nr: string;
      price: string;
      quantity: string;
      total: string;
      quantityContainer: {
        plus: string;
        minus: string;
      }
    }
  }
}